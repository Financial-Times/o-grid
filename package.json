{
  "name": "o-grid",
  "version": "1.0.0",
  "description": "A 12 column responsive, flexbox-based grid system for laying out documents, templates and modules.",
  "main": "main.js",
  "devDependencies": {
    "mocha": "^5.0.0"
  },
  "scripts": {
    "test": "mocha test-sass/"
  }
}
