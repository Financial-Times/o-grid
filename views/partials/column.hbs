<div class="grid-col-{{this.span}}">
	<span class="dev-inner"{{#if this.minHeight}} style="min-height:{{{this.minHeight}}}"{{/if}}>{{@index}}: .grid-col-{{this.span}}</span>
	{{#if this.columns}}
		<div class="grid-row">
			{{#each this.columns}}
				{{> column}}
			{{/each}}
		</div>
	{{/if}}
</div>